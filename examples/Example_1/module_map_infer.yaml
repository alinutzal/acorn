# CommonFramework configuration
stage: graph_construction
model: PyModuleMap
input_dir: /scratch/cf/Example_1/feature_sets/ # Should contain the files used in training and inference
stage_dir: /scratch/cf/Example_1/module_map/ # A directory to be created that will store logs, artifacts, and output data

# Dataset parameters
data_split: [80, 10, 10] # Number of [training, validation, testing] examples
module_map_path: /m4439/Example_1/example_1_module_map.csv #/global/cfs/cdirs
batch_size: 2000000