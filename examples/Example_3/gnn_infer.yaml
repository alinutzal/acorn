stage: edge_classifier
model: InteractionGNN
input_dir: /workspaces/train100events/results/filter/ # Should contain the files used in training and inference
stage_dir: /workspaces/train100events/results/gnn/ # A directory to be created that will store logs, artifacts, and output data
project: CF_Example_3_GNN # Used in logging
gpus: 1
nodes: 1